using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ErgoShop.Utils
{
    /// <summary>
    /// Not used
    /// </summary>
    public static class CollectionFunctions
    {
        //public static bool HasLoop(this LinkedList<Wall> list)
        //{
        //    if (list.Count < 3) return false;
        //    var Head = list.First;
        //    if (Head != null && Head.Next != null)
        //    {
        //        var slow = Head;
        //        var fast = Head.Next;
        //        while (slow.Next != null && fast.Next != null && fast.Next.Next != null)
        //        {
        //            if (slow.Value.Equals(fast.Value))
        //            {
        //                return true;
        //            }
        //            slow = slow.Next;
        //            fast = fast.Next.Next;
        //        }
        //        return false;
        //    }
        //    else
        //    {
        //        return Head != null ? Head.Value.Equals(Head.Next.Value) : false;
        //    }
        //}
    }
}